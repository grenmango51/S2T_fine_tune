# Evaluation outputs

Evaluation commands write detailed JSON results and Markdown summaries here.
The JSON predictions support paired comparisons, metric recomputation, and
baseline reuse. `openrouter_cache/` stores completed cloud responses so repeated
runs can reuse them.

Generated files are excluded from Git. See [evaluation results](../docs/results.md)
for the published aggregate snapshot.
